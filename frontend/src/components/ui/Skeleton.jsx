/** Reusable skeleton block for loading states. Pass className to size it. */
const Skeleton = ({ className = 'h-6 w-full' }) => <div className={`skeleton ${className}`} />;

export default Skeleton;
